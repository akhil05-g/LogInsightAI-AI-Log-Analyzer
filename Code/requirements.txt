# ── Core Web Framework ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
aiofiles>=24.1.0
httpx>=0.27.0

# ── LLM / Gemini / Groq ──
google-genai>=1.0.0
groq>=0.10.0

# ── MCP Protocol ──
mcp>=1.0.0
fastmcp>=2.0.0

# ── Log Parsing Engines ──
drain3>=0.9.11
pygrok>=1.0.0

# ── Detection Engines ──
flashtext>=2.7
pyod>=2.0.0
scikit-learn>=1.4.0
# NOTE: 'logai' and 'torch' are installed in build.sh with fallback handling
# because they have heavy/fragile build dependencies.

# ── Data / ML Core ──
numpy>=1.26.0
pandas>=2.2.0

